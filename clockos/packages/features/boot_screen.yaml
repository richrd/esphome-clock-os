esphome:
  on_boot:
    priority: 100
    then:
      - display.page.show: clockos_page_boot_screen

display:
  - id: !extend clockos_display
    pages:
      - id: clockos_page_boot_screen
        lambda: !include "boot_screen.cpp"

